---
phase: 04-demo-ui
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - ui/pages/home.py
  - ui/app.py
autonomous: false

must_haves:
  truths:
    - "Landing page provides project overview, architecture diagram, and quick-start navigation"
    - "User can navigate from Home to any page and back without losing pipeline results"
    - "Session state persists across all page navigations (UI-07)"
    - "All 6 pages render without import errors and show meaningful content"
    - "The demo is interview-ready: polished, cohesive, and self-explanatory"
  artifacts:
    - path: "ui/pages/home.py"
      provides: "Landing page with project overview, architecture flow, and page navigation cards"
      min_lines: 80
  key_links:
    - from: "ui/pages/home.py"
      to: "ui/pages/*.py"
      via: "st.page_link navigation to all functional pages"
      pattern: "st\\.page_link"
    - from: "ui/app.py"
      to: "st.session_state"
      via: "Session state initialization with defaults"
      pattern: "session_state"
---

<objective>
Build the Home/Landing page and perform final integration polish. The Home page is the first thing the interviewer sees -- it must communicate the project's value proposition, show the architecture at a glance, and provide clear navigation. Final integration ensures all pages work together cohesively with shared session state.

Purpose: The landing page creates the first impression for the interview demo. Integration polish ensures the entire 6-page app works as a cohesive product. This plan also includes a human verification checkpoint to confirm the UI meets demo quality standards.

Output: Complete, polished Home page and verified end-to-end demo UI.
</objective>

<execution_context>
@C:/Users/prave/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/prave/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-demo-ui-for-the-whole-app/04-RESEARCH.md
@.planning/phases/04-demo-ui-for-the-whole-app/04-01-SUMMARY.md
@.planning/phases/04-demo-ui-for-the-whole-app/04-02-SUMMARY.md
@.planning/phases/04-demo-ui-for-the-whole-app/04-03-SUMMARY.md
@.planning/phases/04-demo-ui-for-the-whole-app/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home/Landing page and integration polish</name>
  <files>
    ui/pages/home.py
    ui/app.py
  </files>
  <action>
    1. Replace the stub home.py with a polished landing page:

    **HERO SECTION:**
    - Large title: "ClinIQ" with subtitle "Agentic Clinical Coding & CDI Intelligence Platform"
    - One-line mission in a callout/info box: "A fully local, multi-modal pipeline that ingests clinical data, extracts entities, assigns ICD-10 codes, and detects documentation gaps -- all using OSS models."
    - Three key value propositions in columns:
      * "Multi-Modal Ingestion" -- FHIR R4, text notes, scanned images
      * "Explainable AI" -- full audit trail, chain-of-thought traces
      * "100% Local" -- no API keys, no cloud dependencies

    **ARCHITECTURE OVERVIEW:**
    - Text-based pipeline flow diagram using st.markdown:
      ```
      Clinical Document --> [M1: Ingest] --> [M2: NER] --> [M3: RAG Coding] --> [M4: CDI Agent] --> [M5: Audit Trail]
      ```
    - Brief description of each module (2-3 lines each) in an expandable section

    **MODEL REGISTRY:**
    - Table showing all 5 OSS models with alias, HuggingFace ID, size, and purpose
    - Use st.dataframe or st.table for clean rendering

    **QUICK START NAVIGATION:**
    - Three action cards in columns using st.container with border:
      * "Run the Pipeline" -> st.page_link to Pipeline Runner with description
      * "View Knowledge Graph" -> st.page_link to KG Viewer
      * "Ask Questions" -> st.page_link to QA Bot
    - "Explore Evaluation Metrics" -> st.page_link to Eval Dashboard

    **TECHNOLOGY STACK:**
    - Expandable section listing key technologies: Python, Streamlit, PyTorch, Transformers, FAISS, NetworkX, PyVis, Pydantic v2, Plotly

    2. Update ui/app.py for final integration:
    - Ensure session_state initialization is comprehensive:
      * pipeline_result: None
      * active_case_id: None
      * messages: [] (QA bot)
      * eval_results: None
    - Verify st.set_page_config is called FIRST (before any other st. calls)
    - Add a small "How to Demo" expander in the sidebar with 3 steps:
      1. "Start on Pipeline Runner -- select a demo case"
      2. "Explore KG Viewer and Audit Trail for deep analysis"
      3. "Check Eval Dashboard for metrics, ask QA Bot questions"
    - Ensure sidebar shows current session info: active_case_id if set, pipeline_result status

    **IMPORTANT:**
    - Home page should NOT import any ML modules (no transformers, no torch)
    - Keep it lightweight so it loads instantly
    - Use st.page_link (not st.switch_page) for navigation cards -- page_link creates clickable links, switch_page forces navigation
  </action>
  <verify>
    Start the app with `streamlit run ui/app.py --server.headless true` and verify it starts without errors. Confirm Home page is the default page shown on initial load.
  </verify>
  <done>
    Home page renders with hero section, architecture overview, model registry, and navigation cards. App shell has comprehensive session state initialization and sidebar help. All pages accessible via navigation.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete demo UI</name>
  <files>ui/app.py</files>
  <action>
    Human visually verifies the complete 6-page Streamlit demo UI:
    1. Home -- project overview and navigation
    2. Pipeline Runner -- upload/run/view pipeline results
    3. KG Viewer -- interactive PyVis knowledge graph
    4. Audit Trail -- expandable per-stage decision trace
    5. Eval Dashboard -- metrics with Plotly radar/bar charts
    6. QA Bot -- chat interface with pre-seeded questions

    Verification steps:
    1. Run `streamlit run ui/app.py` from the project root
    2. Verify Home page loads with project overview, architecture diagram, and navigation links
    3. Navigate to Pipeline Runner: select "Case 004: CKD + Hypertension", click "Run Pipeline" with pre-computed toggle ON. If pre-computed data exists, verify results in 4 tabs. If not, verify graceful fallback.
    4. Navigate to KG Viewer: if pipeline result exists, verify colored graph renders. If no result, verify redirect.
    5. Navigate to Audit Trail: if pipeline result exists, verify expandable stage traces. If no result, verify redirect.
    6. Navigate to Eval Dashboard: verify radar chart and per-module bar charts render.
    7. Navigate to QA Bot: click a pre-seeded question, verify correct answer. Type a custom question, verify graceful response.
    8. Navigate between pages and verify session state persists (pipeline result not lost).
  </action>
  <verify>
    Human confirms all 6 pages render correctly and the demo is interview-ready. Type "approved" or describe issues.
  </verify>
  <done>
    All 6 pages verified by human: Home loads with overview, Pipeline Runner shows results, KG Viewer renders graph, Audit Trail shows traces, Eval Dashboard shows charts, QA Bot answers questions. Session state persists across navigation.
  </done>
</task>

</tasks>

<verification>
- All 6 pages render without errors
- Home page is the default landing page
- Navigation between all pages works via sidebar
- Session state persists across page switches
- No ML model imports at page load (only when explicitly triggered)
- QA Bot answers pre-seeded questions correctly
</verification>

<success_criteria>
The complete 6-page Streamlit demo is interview-ready: polished landing page, functional pipeline runner with results display, interactive KG visualization, expandable audit trail, metrics dashboard with charts, and responsive QA bot. All pages share session state and navigate cohesively.
</success_criteria>

<output>
After completion, create `.planning/phases/04-demo-ui-for-the-whole-app/04-05-SUMMARY.md`
</output>
