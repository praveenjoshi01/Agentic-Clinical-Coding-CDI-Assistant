---
phase: 04-demo-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/app.py
  - ui/components/__init__.py
  - ui/components/theme.py
  - ui/components/metric_cards.py
  - ui/components/entity_highlight.py
  - ui/components/code_display.py
  - ui/components/graph_embed.py
  - ui/components/pipeline_status.py
  - .streamlit/config.toml
  - scripts/precompute_demo.py
  - ui/demo_data/demo_questions.json
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "streamlit run ui/app.py starts without errors and shows sidebar navigation"
    - "All 6 page stubs render a title when navigated to"
    - "Pre-computed demo results exist as JSON files loadable by PipelineResult.model_validate"
    - "Custom theme applies green primary color and wide layout"
  artifacts:
    - path: "ui/app.py"
      provides: "Streamlit entry point with st.navigation, page config, session state init"
      min_lines: 40
    - path: "ui/components/theme.py"
      provides: "Custom CSS injection and theming constants"
      exports: ["inject_custom_css", "ENTITY_COLORS"]
    - path: "ui/components/entity_highlight.py"
      provides: "NER entity annotation rendering with overlap resolution"
      exports: ["render_ner_highlights"]
    - path: "ui/components/code_display.py"
      provides: "ICD-10 code result card rendering"
      exports: ["render_code_cards", "render_principal_diagnosis"]
    - path: "ui/components/graph_embed.py"
      provides: "PyVis HTML generation and st.components embedding"
      exports: ["render_kg_graph"]
    - path: "ui/components/metric_cards.py"
      provides: "Reusable metric display with st.metric"
      exports: ["render_metric_row"]
    - path: "ui/components/pipeline_status.py"
      provides: "Pipeline progress wrapper using st.status"
      exports: ["run_pipeline_with_status"]
    - path: "scripts/precompute_demo.py"
      provides: "Script to generate pre-computed PipelineResult JSON files"
      min_lines: 30
    - path: ".streamlit/config.toml"
      provides: "Streamlit theme configuration"
      contains: "primaryColor"
    - path: "ui/demo_data/demo_questions.json"
      provides: "Pre-seeded QA bot questions with expected answers"
  key_links:
    - from: "ui/app.py"
      to: "ui/pages/*.py"
      via: "st.navigation with st.Page references"
      pattern: "st\\.Page.*pages/"
    - from: "scripts/precompute_demo.py"
      to: "cliniq/pipeline.py"
      via: "run_pipeline_audited import"
      pattern: "from cliniq\\.pipeline import"
    - from: "ui/components/entity_highlight.py"
      to: "cliniq/models/entities.py"
      via: "ClinicalEntity type usage"
      pattern: "entity_type|start_char|end_char"
---

<objective>
Build the Streamlit app shell, all reusable UI components, theme configuration, and pre-computed demo data generation.

Purpose: Establishes the complete UI foundation so all page plans (02-05) can focus purely on page-specific logic. Components are built once here and imported by all pages. Demo data is pre-computed so pages can load instantly without running the full pipeline.

Output: Working app shell with navigation to 6 stub pages, 7 reusable components, custom theme, pre-computed demo JSON files, and QA bot question bank.
</objective>

<execution_context>
@C:/Users/prave/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/prave/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-demo-ui-for-the-whole-app/04-RESEARCH.md
@cliniq/pipeline.py
@cliniq/models/__init__.py
@cliniq/models/entities.py
@cliniq/models/coding.py
@cliniq/models/cdi.py
@cliniq/models/audit.py
@cliniq/models/document.py
@cliniq/knowledge_graph/schema.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: App shell, theme, navigation, and reusable components</name>
  <files>
    ui/app.py
    ui/pages/home.py
    ui/pages/pipeline_runner.py
    ui/pages/eval_dashboard.py
    ui/pages/kg_viewer.py
    ui/pages/audit_trail.py
    ui/pages/qa_bot.py
    ui/components/__init__.py
    ui/components/theme.py
    ui/components/metric_cards.py
    ui/components/entity_highlight.py
    ui/components/code_display.py
    ui/components/graph_embed.py
    ui/components/pipeline_status.py
    .streamlit/config.toml
    pyproject.toml
  </files>
  <action>
    1. Update pyproject.toml to add UI dependencies: streamlit>=1.55.0, plotly>=5.20.0, pyvis>=0.3.2, st-annotated-text>=4.0.2, networkx>=3.3. Add networkx and pyvis to main deps (networkx may already be transitive via the KG module but make it explicit).

    2. Create `.streamlit/config.toml` with theme:
       - primaryColor="#2ecc71" (green), backgroundColor="#ffffff", secondaryBackgroundColor="#f0f2f6", textColor="#1a1a2e", font="sans serif"

    3. Create `ui/app.py` as the Streamlit entry point:
       - Use st.set_page_config(page_title="ClinIQ - Clinical Documentation Integrity", page_icon=":material/medical_services:", layout="wide") -- this MUST come before st.navigation
       - Define 6 pages using st.Page: Home (default), Pipeline Runner, Eval Dashboard, KG Viewer, Audit Trail, QA Bot
       - Group in st.navigation: "Overview" = [Home], "Pipeline" = [Pipeline Runner, KG Viewer, Audit Trail], "Analysis" = [Eval Dashboard, QA Bot]
       - Initialize session state defaults before pg.run(): pipeline_result=None, active_case_id=None, messages=[] (for QA bot chat history), eval_results=None
       - Add sidebar footer with "ClinIQ v0.1.0" and "Powered by OSS Models"
       - Call pg.run() at the end

    4. Create 6 stub page files in `ui/pages/` -- each with just `import streamlit as st` and `st.title("Page Name")` plus `st.info("Coming in Plan 04-0X")`. These are placeholders replaced in later plans.

    5. Create `ui/components/__init__.py` exporting all component modules.

    6. Create `ui/components/theme.py`:
       - ENTITY_COLORS dict: diagnosis="#ff6b6b", procedure="#4ecdc4", medication="#45b7d1", anatomical_site="#96ceb4", qualifier="#ffd93d", lab_value="#c9b1ff"
       - STATUS_COLORS dict: green="#2ecc71", amber="#f39c12", red="#e74c3c"
       - inject_custom_css() function using st.markdown with unsafe_allow_html for minor tweaks only (entity annotation padding, expander spacing). Do NOT target auto-generated Streamlit class names.

    7. Create `ui/components/metric_cards.py`:
       - render_metric_row(metrics: dict[str, tuple[str, str | None]]) that creates st.columns and st.metric for each item. Each metric is (value, delta). Handles 2-6 metrics in a row.

    8. Create `ui/components/entity_highlight.py`:
       - render_ner_highlights(narrative: str, entities: list) function
       - Sort entities by start_char. Resolve overlaps: if two entities overlap, keep the one with higher confidence (or the longer one if confidence is equal). Build parts list of plain text strings and (text, label, color) tuples. Use annotated_text(*parts) from annotated_text library.
       - Prefix "NEG " to label if entity.negated is True
       - Use ENTITY_COLORS from theme.py

    9. Create `ui/components/code_display.py`:
       - render_principal_diagnosis(code: CodeSuggestion | None) -- renders a prominent card with code, description, confidence bar, reasoning, and needs_specificity badge
       - render_code_cards(codes: list[CodeSuggestion], title: str) -- renders a grid of code cards with code, description, confidence, and evidence text in expanders

    10. Create `ui/components/graph_embed.py`:
        - render_kg_graph(G: nx.DiGraph, case_codes: list[str], cdi_report: CDIReport | None, height: int = 600)
        - Filter to case-relevant subgraph: only nodes in case_codes + 1-hop neighbors in KG
        - Color nodes: red if in conflict_codes, amber if in gap_codes, green otherwise (VIZ-02)
        - Set node title to description from G.nodes[code].get("description", code) for click tooltip (VIZ-03)
        - If CDI report has physician queries for a gap code, append query text to node title
        - Add edges between case-relevant nodes with edge title = relation type
        - Use net.barnes_hut() for physics, generate_html(), embed with st.components.v1.html()
        - CRITICAL: Never render full KG (70k nodes). Always filter to case subgraph (VIZ-04).

    11. Create `ui/components/pipeline_status.py`:
        - run_pipeline_with_status(input_data, use_llm_queries=False, skip_cdi=False) -> PipelineResult
        - Wraps run_pipeline_audited inside st.status container
        - Shows per-stage progress: "Stage 1: Ingesting...", "Stage 2: NER...", etc.
        - On completion: status.update(label="Pipeline complete!", state="complete")
        - Stores result in st.session_state["pipeline_result"]
        - Uses @st.cache_resource for ModelManager to prevent reloading models on every rerun
  </action>
  <verify>
    Run `pip install -e ".[dev]" streamlit plotly pyvis st-annotated-text` and then `streamlit run ui/app.py --server.headless true` -- app should start without import errors. Verify all 6 pages are listed in sidebar navigation. Stop the server after confirming startup.
  </verify>
  <done>
    App starts, sidebar shows 6 pages grouped into Overview/Pipeline/Analysis, each stub page renders its title, .streamlit/config.toml exists with green theme, all 7 component modules import without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pre-computed demo data and QA bot question bank</name>
  <files>
    scripts/precompute_demo.py
    ui/demo_data/demo_questions.json
  </files>
  <action>
    1. Create `scripts/precompute_demo.py`:
       - Import run_pipeline_audited from cliniq.pipeline
       - Define 3 demo cases covering all modalities:
         * case_004.txt (text note -- CKD + Hypertension)
         * case_010.txt (text note -- CHF + AFib)
         * case_001.json (FHIR bundle)
       - For each case: run run_pipeline_audited(input_data, use_llm_queries=False) to get PipelineResult
       - Serialize each result to JSON using result.model_dump_json(indent=2)
       - Write to ui/demo_data/precomputed/{case_id}.json
       - Include a --quick flag that runs with skip_coding=True for fast generation (NER + CDI only)
       - Print summary: case_id, source_type, entity_count, code_count, gap_count, processing_time
       - Handle encoding issues (latin-1 fallback for text files, as in scripts/demo.py)

    2. Create `ui/demo_data/demo_questions.json` with 7+ pre-seeded QA bot questions and expected answers:
       - "What models does ClinIQ use?" -> list all 5 models with aliases and purposes
       - "How does the pipeline work?" -> describe 5-stage pipeline flow
       - "What is the CDI agent?" -> explain KG-based gap detection, conflict detection, physician queries
       - "How does the RAG coding module work?" -> FAISS retrieval + cross-encoder reranking + Qwen reasoning
       - "What evaluation metrics does ClinIQ track?" -> NER F1, Top-3 accuracy, MRR, query relevance, CoT coherence
       - "What data formats does ClinIQ support?" -> FHIR R4 JSON, plain text clinical notes, scanned images (PNG/JPG)
       - "How does the knowledge graph work?" -> NetworkX DiGraph with 4 edge types, curated rules, co-occurrence/conflict/qualifier analysis
       Each answer should be 2-4 sentences, factually accurate based on the codebase.

    3. Create `ui/demo_data/` directory and `ui/demo_data/precomputed/` directory. Add empty __init__.py if needed for imports.

    NOTE: The precompute script requires ML models to be downloaded (~2.1GB). It does NOT need to be run during plan execution. The script is provided so the user can run it when ready. For plan verification, just confirm the script is syntactically valid with `python -c "import scripts.precompute_demo"` or by checking syntax.
  </action>
  <verify>
    Run `python -c "import ast; ast.parse(open('scripts/precompute_demo.py').read()); print('syntax OK')"` to verify script syntax. Run `python -c "import json; json.load(open('ui/demo_data/demo_questions.json')); print('JSON OK')"` to verify questions file. Verify ui/demo_data/precomputed/ directory exists.
  </verify>
  <done>
    scripts/precompute_demo.py exists with correct pipeline imports and 3 demo case definitions. ui/demo_data/demo_questions.json contains 7+ questions with accurate answers. ui/demo_data/precomputed/ directory exists ready for pre-computed results.
  </done>
</task>

</tasks>

<verification>
- `streamlit run ui/app.py --server.headless true` starts without errors
- Sidebar shows 3 groups: Overview, Pipeline, Analysis with 6 pages
- All component modules importable: `python -c "from ui.components import theme, metric_cards, entity_highlight, code_display, graph_embed, pipeline_status"`
- .streamlit/config.toml has green theme
- scripts/precompute_demo.py has valid syntax
- ui/demo_data/demo_questions.json has 7+ Q&A pairs
</verification>

<success_criteria>
App shell starts and navigates between 6 stub pages. All reusable components are importable. Theme is configured. Demo data infrastructure is ready for population.
</success_criteria>

<output>
After completion, create `.planning/phases/04-demo-ui-for-the-whole-app/04-01-SUMMARY.md`
</output>
