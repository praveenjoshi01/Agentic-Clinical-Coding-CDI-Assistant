---
phase: 04-demo-ui
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - ui/pages/kg_viewer.py
  - ui/pages/audit_trail.py
autonomous: true

must_haves:
  truths:
    - "User sees interactive PyVis graph with color-coded nodes (green/amber/red) for the current case"
    - "User clicks a node in the graph and sees code description, evidence text, and CDI query if applicable"
    - "Graph shows only case-relevant subgraph, not all 70k ICD-10 codes"
    - "User sees expandable per-stage audit trail with timing, I/O summaries, CoT traces, and evidence spans"
    - "Both pages gracefully handle missing session state with redirect to Pipeline Runner"
  artifacts:
    - path: "ui/pages/kg_viewer.py"
      provides: "KG Viewer page with PyVis graph embedding and CDI status coloring"
      min_lines: 80
    - path: "ui/pages/audit_trail.py"
      provides: "Audit Trail page with expandable stage traces and evidence spans"
      min_lines: 80
  key_links:
    - from: "ui/pages/kg_viewer.py"
      to: "ui/components/graph_embed.py"
      via: "render_kg_graph import and call"
      pattern: "render_kg_graph"
    - from: "ui/pages/kg_viewer.py"
      to: "cliniq/knowledge_graph/builder.py"
      via: "build_cdi_knowledge_graph for graph data"
      pattern: "build_cdi_knowledge_graph"
    - from: "ui/pages/kg_viewer.py"
      to: "st.session_state[pipeline_result]"
      via: "Reads case codes and CDI report from session state"
      pattern: "session_state.*pipeline_result"
    - from: "ui/pages/audit_trail.py"
      to: "st.session_state[pipeline_result]"
      via: "Reads audit_trail from session state"
      pattern: "session_state.*pipeline_result.*audit_trail"
---

<objective>
Build the KG Viewer page (interactive knowledge graph visualization) and the Audit Trail page (expandable pipeline decision trace). Both pages read from session state populated by the Pipeline Runner.

Purpose: KG Viewer delivers the visual "wow factor" for the interview demo -- showing the clinical reasoning graph with color-coded CDI status. Audit Trail provides complete transparency into every pipeline decision. Together they fulfill VIZ-01 through VIZ-04 and UI-03, UI-04.

Output: Two fully functional pages at ui/pages/kg_viewer.py and ui/pages/audit_trail.py.
</objective>

<execution_context>
@C:/Users/prave/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/prave/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-demo-ui-for-the-whole-app/04-RESEARCH.md
@.planning/phases/04-demo-ui-for-the-whole-app/04-01-SUMMARY.md
@cliniq/knowledge_graph/__init__.py
@cliniq/knowledge_graph/builder.py
@cliniq/knowledge_graph/schema.py
@cliniq/models/cdi.py
@cliniq/models/audit.py
@cliniq/pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: KG Viewer page with PyVis graph and CDI status coloring</name>
  <files>ui/pages/kg_viewer.py</files>
  <action>
    Replace the stub kg_viewer.py with the full implementation.

    **SESSION STATE CHECK:**
    - If "pipeline_result" not in st.session_state or result is None:
      * st.warning("No pipeline result available. Run the pipeline first.")
      * st.page_link("pages/pipeline_runner.py", label="Go to Pipeline Runner", icon=":material/play_arrow:")
      * st.stop()

    **PAGE LAYOUT:**
    - st.title("Knowledge Graph Viewer") with subtitle explaining color coding
    - Color legend in a horizontal row: green circle = "Well Documented", amber circle = "Needs CDI Query", red circle = "Conflict Detected" (use st.columns with colored markdown or st.html for small colored circles)

    **GRAPH RENDERING:**
    - Extract case codes from pipeline_result.coding_result:
      * principal_diagnosis.icd10_code (if exists)
      * [c.icd10_code for c in secondary_codes]
      * [c.icd10_code for c in complication_codes]
    - Build or load the CDI knowledge graph using build_cdi_knowledge_graph()
      * Cache with @st.cache_resource so it's built only once per session
    - Call render_kg_graph(G, case_codes, result.cdi_report, height=600) from graph_embed component
    - Below the graph, add an expandable "Graph Details" section showing:
      * Node count, edge count in subgraph
      * List of all case codes with their CDI status (gap/conflict/ok)

    **CDI SUMMARY SIDEBAR:**
    - In a sidebar or right column (2-column layout):
      * Documentation gaps count, conflict count, missed diagnoses count
      * For each gap: show code, missing qualifier, physician query in an expander
      * For each conflict: show code_a vs code_b with recommendation

    **IMPORTANT:**
    - Use @st.cache_resource for build_cdi_knowledge_graph() to avoid rebuilding on every rerun
    - The graph_embed component already handles subgraph filtering -- just pass case_codes
    - If coding_result has no codes (pipeline ran with skip_coding), show st.info message
  </action>
  <verify>
    Run `python -c "import ast; ast.parse(open('ui/pages/kg_viewer.py').read()); print('syntax OK')"` to verify syntax. Check imports resolve: `python -c "from cliniq.knowledge_graph import build_cdi_knowledge_graph; print('import OK')"`.
  </verify>
  <done>
    KG Viewer page renders interactive PyVis graph with green/amber/red node coloring based on CDI status. Nodes show tooltips with code description and CDI query. Graph filters to case-relevant subgraph only. Gracefully handles missing session state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit Trail page with expandable stage traces</name>
  <files>ui/pages/audit_trail.py</files>
  <action>
    Replace the stub audit_trail.py with the full implementation.

    **SESSION STATE CHECK:**
    - If "pipeline_result" not in st.session_state or result is None:
      * st.warning("No pipeline result available. Run the pipeline first.")
      * st.page_link("pages/pipeline_runner.py", label="Go to Pipeline Runner", icon=":material/play_arrow:")
      * st.stop()
    - If result.audit_trail is None:
      * st.info("No audit trail available for this pipeline run. The pipeline may have been run without auditing.")
      * st.stop()

    **PAGE LAYOUT:**
    - st.title("Audit Trail") with subtitle "Complete pipeline decision trace"
    - Header metrics row: Case ID, Timestamp, Total Stages, Total Processing Time

    **STAGE TRACES:**
    - For each trace in audit_trail.stages:
      * Stage number icon: ingestion=1, ner=2, rag=3, cdi=4
      * st.expander with label: "Stage {N}: {STAGE_NAME} ({processing_time_ms:.1f}ms)"
      * Inside expander:
        - Two-column layout: Input Summary (left), Output Summary (right) as st.metric
        - If trace.details exists (non-empty dict): render as st.json or formatted key-value pairs
        - If trace.cot_traces is non-empty:
          * st.markdown("**Chain-of-Thought Reasoning:**")
          * For each CoT trace: st.code(cot, language="text") with scroll
        - If trace.retrieval_logs is non-empty:
          * st.markdown("**Retrieval Logs:**")
          * For each log: st.json with query, selected_code, selected_confidence
          * Optionally: expandable top-k and reranked results

    **EVIDENCE SPANS:**
    - Below stage traces, a separate section "Evidence Attribution"
    - For each code in audit_trail.evidence_spans:
      * st.expander with code as label
      * Show count of supporting spans
      * For each span: render as blockquote (st.markdown(f"> {span}"))
      * Truncate very long spans to 300 chars with "..." indicator

    **TIMELINE VISUALIZATION (optional polish):**
    - If time permits: add a simple horizontal bar chart (st.bar_chart or plotly) showing per-stage processing time breakdown. This gives a visual sense of where time is spent.

    **IMPORTANT:**
    - All data comes from st.session_state["pipeline_result"].audit_trail
    - This page is read-only -- no mutations to session state
    - Handle edge cases: stages with no CoT, stages with no retrieval logs, empty evidence spans
  </action>
  <verify>
    Run `python -c "import ast; ast.parse(open('ui/pages/audit_trail.py').read()); print('syntax OK')"` to verify syntax.
  </verify>
  <done>
    Audit Trail page renders expandable stage traces for all pipeline stages with timing, I/O summaries, CoT traces, retrieval logs, and evidence spans. Gracefully handles missing audit trail and empty fields.
  </done>
</task>

</tasks>

<verification>
- Both pages have valid Python syntax
- Both pages check for session state and show redirect when pipeline_result is missing
- KG Viewer imports build_cdi_knowledge_graph and render_kg_graph correctly
- Audit Trail iterates over audit_trail.stages and evidence_spans correctly
- Neither page mutates session state (read-only)
</verification>

<success_criteria>
KG Viewer shows interactive color-coded PyVis graph for case subgraph with node tooltips. Audit Trail shows expandable per-stage traces with timing, CoT, and evidence. Both pages redirect to Pipeline Runner when no data is available.
</success_criteria>

<output>
After completion, create `.planning/phases/04-demo-ui-for-the-whole-app/04-03-SUMMARY.md`
</output>
