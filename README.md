# Agentic-Clinical-Coding-CDI-Assistant
The core idea is a small multi-agent pipeline that takes a synthetic clinical note, extracts diagnoses and procedures using NLU, suggests ICD-10 codes via RAG, and then runs a CDI agent to flag documentation gaps — all with an explainability layer. Each module is independently demonstrable.
